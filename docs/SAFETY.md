# Safety

The hazard matrix: every way this firmware can hurt the equipment or the
site, the invariant that prevents it, the mechanism that holds the
invariant, the test that proves the mechanism, the evidence that the test
was run, and who reviewed it. A row whose evidence column says *compiles* is
a row that is not done; a row whose test column is empty is a hazard nobody
has argued against yet. The shape is the engineering repository's
[change record](https://github.com/origin89hq/engineering/blob/main/templates/safety/change-record.md);
a change that alters a row files one.

Safe is not always de-energised. The generator contact's safe state is open;
a frost heater's is a decision the configuration makes per output (F-011). The
comms processor's rail is on (F-014). What every row shares is that the state
is *declared*, before the output exists.

## Hazards

| # | Hazard | Invariant | Mechanism | Test | Evidence | Reviewer |
| --- | --- | --- | --- | --- | --- | --- |
| H1 | The generator's start contact closes with no firmware running: an empty flash boots the ST system bootloader, which pulls `RUN` up and can pulse `KICK` (hardware#30) | There is never an empty-flash window, and no image reaches its second instruction with `RUN` undriven | The bootloader never erased after manufacture, `nSWAP_BANK` kept at 1 so bank 1 and the bootloader stay at `0x08000000`, and an update copied in only from a verified image staged on the NOR (F-070); `RUN`/`KICK` low at the reset vector (F-001); revision B moves the lines and adds pull-downs (`A-34`) | M1: with board B on CN9 and nothing on its CN10, a mass erase, a watchdog reset, a panic and a supply cut, the contact scoped and never closing; M7: an update copy and a rollback copy each interrupted at every step, a NOR removed mid-copy, and RM0444's empty check read for dual-bank mode | host: `f_001_*` reads the declared fail state in `o89-core`, 2026-09-18; bench 2026-09-18: the production image boots through the bootloader on board A revision A, the lines not yet scoped (`docs/bench/2026-09-18.md`), in the 8 KB layout before #185; bench 2026-09-26: `nSWAP_BANK` read 1 on board A, the 32 KB layout boots through the bootloader into `0x08008000`, and a production flash onto the 8 KB layout is refused before it writes (`docs/bench/2026-09-26.md`); lines not scoped, no image across the bank boundary | — |
| H2 | A reset stops a running generator: on revision A any controller reset opens the contact within milliseconds (hardware#18) | A generator that was running under automatic control resumes when its condition still holds; a manual start does not | The run reason in FRAM before the output moves (F-022): the output driver moves on a `Declared` that only a landed write produces; the resume policy (F-061); board B's ride-through window on revision B | M8 on the bench with the analyser on CN9; boot-to-first-kick posted (F-015) | host: `f_022_*` in `o89-core`, 2026-09-18; `f_022_*` in `o89-sim`, 2026-09-23: a start and a stop cut at every byte leave the reason before or the one declared, and a cut `declare` never hands out a `Declared` the part does not hold; bench: none yet | — |
| H3 | The comms rail switched on after minutes off corrupts the STM32's control flow (hardware#5), which on this board holds the generator lines | The rail is never switched on after a long off on revision A, and a reset puts it back before any bus | F-005, F-006; the default-on switch on revision B (F-014) | M1: the ladder's rungs exercised with the rail scoped; the 22-of-22 case never produced | bench 2026-09-14 and 2026-09-16 record the hazard, not yet the mitigation; host: `f_004_*` and `f_005_*` in `o89-core` run the rail sequencer through a boot, a cut and the third rung on both revisions, 2026-09-18; `l_110_*`, `l_111_*` and `l_112_*` in `o89-sim` run the link's ladder into it against a peer that never answers; bench 2026-09-19 §4: three 5 s cuts at sixty seconds of silence on board A, each recorded, the lines unscoped: the log shows what the firmware asked of the pins, and neither the order they moved in nor the five seconds off was measured, so the mitigation stays unverified until the scoped run of the rungs and the repeated supply cuts; the same section: after a reset the rail goes on in the log's first millisecond and the store after its settle, the path before the log's clock unmeasured; host: `f_017_*` and `f_018_*` carry the ladder's cuts across controller resets, a cut of their record at every byte keeps the cuts before it or after it, and three revision A boots whose ladder writes after the first are each cut at every byte still reach the third rung; a reset before a boot's count lands stays uncounted; §8: the third rung reached on board A across a controller reset, the lines unscoped; §9: on board A the cut made on the turn its count landed, the rail off 5 000 ms from there by the log, counts late and refused leaving the rail on, and a download's reset served in the turn a count landed, the lines unscoped; host 2026-09-24: `l_035_*` in `o89-core` and `o89-sim`: a controller without a device secret keeps the link down for ten minutes against a module stating itself and cuts nothing, and `l_111_a_unit_with_its_device_id_still_cuts_a_silent_module` keeps the cut for a unit that has one; bench: none | — |
| H4 | A brown-out mid-write destroys the counters that stop a replay, or the configuration that runs the site: seven brown-outs on 2026-09-16 wiped both counter slots | A write cut at any byte leaves the previous record in effect, and no replay counter regresses under the same key material | A/B slots with the magic landed last (F-020); no transaction after the PVD edge (F-021); crash-at-every-step on the host (F-025); the counter and the dedup entry in one record, the RAM copy moved only once the part has it (P-079, P-080); the epoch stamp that finishes a cut reset (F-026) | M2: a thousand supply cuts mid-write overnight | host: `f_025_*`, `p_080_*`, `p_085_*` and `f_026_*` in `o89-core` and `o89-sim`, 2026-09-18; a signed command through `admit` and step 6, cut at every step, permitted only once its counter and entry landed, 2026-09-23; signed SetConfig through counter admission and section commit, cut at every storage byte (`p_102_*`), plus `p_085_healthy_network_reset_scrubs_both_slots_after_reboot` and `l_135_healthy_reset_cut_at_every_byte_scrubs_credentials_before_epoch_and_on_retry` inspecting the complete network reservation and proving an advanced epoch never retains old credentials, 2026-09-23; `p_085_damaged_network_reset_*` in `o89-sim` erases both damaged slots and retries reset across every byte-level cut, including an absent read with residual bytes; `p_108_p_100_*` in `o89-core` and `o89-sim` reads corrupt and malformed sections as version zero with no body, preserves damaged bytes on reads and stale writes, and replaces only at expected version zero, including signed writes through the session, 2026-09-24; host: `l_133_unwritten_master_clears_a_foreign_cache_including_after_damaged_reset`, `l_133_unwritten_master_does_not_clear_a_module_already_at_zero`, `l_133_unanswered_unwritten_clear_retries_the_same_request` and `l_137_failed_unwritten_clear_is_retried_at_the_next_link_up` exercise the metadata-free clear through HostileComms; comms `l_133_unwritten_clear_*` and `l_137_unwritten_clear_*` cover durable erasure and failed-erase retry, 2026-09-24; unwritten-clear Wi-Fi shutdown and subsequent reconfiguration have no bench evidence; host 2026-09-24: `f_041_secret_replacement_*` cuts staging and recovery at every byte, keeping the old secret/counter or the fresh secret with a mintable counter before sessions exist; `f_041_discover_after_secret_replacement_over_a_corrupt_counter_has_a_challenge` exercises Discover through HostileComms; corrupted intent is discarded and reported without changing the active pair, with erasure interrupted at every byte; secret replacement bench verification and independent review remain outstanding; bench: the boot count landed through every reset of 2026-09-18 §3 | — |
| H5 | A hung task leaves an output where it was while the watchdog is fed by a timer | The watchdog is fed only when every task has checked in inside its period | The rollcall supervisor (F-007), on its own interrupt (`CEC` at `P8`) above the control executor (`P12`) and thread mode, so a task that blocks either is still named, with a compile-time assertion refusing a priority the M0+ does not implement; the blame that survives the reset (F-008) | M1: deliberate starvation, the reset, the next boot naming the task | host: `f_007_*` and `f_008_*` in `o89-core` run the rollcall and the last words on a laptop; `f_032_*` in `o89-comms-core` checks independent Wi-Fi/BLE progress, each stalled task and the 6,000 ms expiry, with comms starvation still untested on the bench; bench 2026-09-18 §1: a task that stopped checking in was named and the reset followed; bench 2026-09-18 §3: a recorder blocked in a DMA transfer's await was named 101 ms past its window and the watchdog reset the part, the firmware sampling the generator contact open before and after and the lines themselves unmeasured, which proves the cooperative case only; bench 2026-09-19: with the control task spinning the thread executor, the supervisor still ran from its interrupt, named Control 99 ms past its window, and the boot after the watchdog read the blame; bench 2026-09-25 (#176), after control moved to its own interrupt executor: at embassy's `P2`/`P3` every priority truncated to 0 on the M0+, the spinning control executor blocked the time driver and the supervisor, and the boot after the watchdog read no blame; at `P8`/`P12` the supervisor named Control 15 ms past its window, the watchdog reset the part about 8 s later, the next boot read the blame, and the panic proof after it named its site | — |
| H6 | A decision reaches an output without authority: a behaviour writes a pin, or an output arrives granted | No behaviour touches an output; every output arrives in shadow | `Decision` is `#[must_use]` and inert (F-060); per-output shadow (F-011) | M6: a test per behaviour that fails when its fail-safe is deleted; a month of shadow compared against what happened | none yet | — |
| H7 | The comms processor makes a decision, or a client makes one through it | The untrusted half cannot reach the crate that decides; every write is authenticated at the controller | `o89-comms` never depends on `o89-core` (F-081); KM43's MACs and counters at the controller, in P-080's order, with the operation reachable only through a permit issued after the counter lands (P-079, P-084); a verified request whose `req_id` the session accepted already or that is below its window dropped before the counter is read, which bounds how long a withheld signed write stays playable (P-022) | The gate, watched refuse the dependency; M4's adversarial corpus | gate: 2026-09-18, refused `o89-core` under `o89-comms`; host: `p_080_*`, `p_084_*`, `p_120_*` and `p_124_*` in `o89-core`, every single-bit flip of a signed command refused with nothing written, 2026-09-23; dispatched from the session on the controller's link, `Command` is answered `rejected` while no output has authority, host: `p_051_*`, `p_077_*`, `p_079_*`, `p_080_*` and `p_084_*` in `session.rs`, 2026-09-23; `SetConfig` and `Time` are served: `p_079_config_with_bad_mac_or_refused_counter_cannot_write_a_section`, `p_100_signed_config_spends_counter_before_version_and_validation`, `p_110_a_client_time_spends_its_counter_and_is_handed_to_the_recorder` and `p_118_a_second_time_while_one_is_decided_is_7_and_a_busy_answer_is_7_under_the_key` in `session.rs`; host 2026-09-24: `sessions::adversarial` rejects cross-session stamps and foreign Hello, Pair and signed-write proofs, with each MAC guard removed and the corresponding test observed failing; capability checks, the mask being the slot's role's row and never its `client_kind` (P-105, P-250): `p_105_the_mask_is_the_role_row_whatever_the_client_said_it_was` in `clients.rs`, `p_105_p_250_a_later_label_pairing_is_an_admin_refused_a_network_write_sealed`, `p_105_p_250_a_cloud_kind_paired_first_is_the_owner_and_may_write_the_network` and `p_105_p_251_a_viewer_is_sealed_error_20_for_the_network_section_and_the_scan_list` in `session.rs`, host 2026-09-25; `Firmware` is the only unserved signed type; a verified command replayed by the comms processor acts once and is answered once, host: `p_022_*` in `o89-core` and `o89-sim`, 2026-09-23 | — |
| H8 | The module can no longer be reprogrammed on revision A: a bad image takes the only wire-free download path with it (#1) | The download window runs before any code that can crash, and is never triggered from the client stream | F-032, F-033, F-034; OTA rollback to the image that honoured it, by a bootloader built with rollback and a confirmation only the window's run can make (F-088, F-089); the factory slot (F-036, F-074) | M3: a crashing OTA image recovered through the STM32 with no wire, then one with the handler omitted | bench 2026-09-19 §5: the window entered from the controller's knock with no wire, esptool connected and erased, and the RTC watchdog the firmware had left armed reset the loader eight seconds in, which took the app with it; the strap route with the IO8 wire recovered the module twice; with the window disarming the watchdog first (F-034), six flashes through it with no wire, each hash-verified, and a bridge abandoned by its host reclaimed by the next flash or closed by its lapsed lease, the module back on the link each time; §7: an image of ours that reset after every window replaced through that window with no wire; `espflash`'s bootloader found built without rollback, from its manifest and ESP-IDF's default, so the `New` state the bench wrote held nothing until the bootloader of F-088; host: `f_088_*` in `o89-dev` refuse a module holding any other bootloader before a byte is written, `f_089_*` in `o89-comms-core` make the proof only once the window has passed and a compile-fail test shows nothing else can make it; bench 2026-09-22: the rollback bootloader put on the module by the knock, then #1's acceptance test run: the crashing image written into `ota_0` and the next boot's text reading `Defaulting to factory image`, the link up on the factory build with the crash loop's ROM text counted, and the ordinary image back through the factory window with no wire; the windowless image linked stating `-nw`, and the next knock's reset had the bootloader boot the factory image, whose window answered; the `otadata` bytes themselves not read back between steps, and the watchdog's reset path not run separately | — |
| H9 | A clock moved by a client or the comms processor ages out a dedup entry, a session or a challenge, and a retried command starts the generator twice | No duration is measured on the wall clock | The monotonic tick (P-004); every bound in `o89-core` on it | `p_004_*` in `o89-core`; M4: the clock moved eleven minutes under a running session and nothing on the tick moves | `o89-core` tests, 2026-09-18; host 2026-09-24: `o89-sim::sessions::adversarial::p_004_clock_moved_eleven_minutes_under_a_session_does_not_move_the_tick` drives a signed eleven-minute set through HostileComms, checks the unchanged tick, replay refusal and original idle expiry; simulated successful recorder writes, no RTC bench evidence | — |
| H10 | A hung instrument reads as a steady site: a bus keeps answering the same plausible number for a week | A channel that has not changed inside its declared run reports `stale` | The store's maximum unchanged run per channel; `initialising`, `sensor_fault`, `absent` as distinct validities | M5: a frozen fixture crosses its run and the reading turns stale; a reconnected probe starts a new run | host 2026-09-26: `o89-core` `signals` tests turn a slot stale past its maximum age and past its unchanged run while it keeps being written, restart the run on a changed value, a changed quality and a return from silence, and keep a channel with no run limit current for a week of equal writes; no driver or bus is wired to the store yet, and there is no simulated season or bench evidence | — |
| H11 | An estimate is acted on as a measurement: an EPEver's computed state of charge starts the generator under a `counted_only` rule | Provenance is declared per cell and never defaults to measured | F-052 | M5: a cell declared `estimated` refused by `counted_only` (#5) | host 2026-09-26: `f_052_*` in `o89-core` and `o89-drivers`: a checked register map refuses a state of charge declared `measured` and any `derived`, `commanded` or missing provenance, at build time for a `const` table; decoding writes each cell's declared provenance, and `counted_only` refuses the estimate while `estimate_accepted` takes it; a polled state of charge below 0 % or above 100 % publishes `out_of_range` and is never eligible; an EPEver's state of charge at `0x311A` publishes only once the caller declares the unit's scale, which the vendor's two documents give differently, and then as `estimated`: `epever::tests::f_052_a_declared_state_of_charge_is_an_estimate_counted_only_refuses` polls it through the port seam from hand-built frames and finds it refused by `counted_only`, and undeclared it publishes `unsupported`; no generator behaviour reads it, and no EPEver has been read on the bench | — |
| H12 | An image that fits the part and not the slot ships and fails its first update in a cabin | Every image is measured as the `.bin` against its slot with a margin, on every commit | F-080 | The gate, watched refuse a lowered budget | gate: 2026-09-18 | — |
| H13 | A start attempt below −20 °C damages the choke actuator, and the failure is silent until the engine will not catch in January | The decision is made deliberately, from a temperature the controller holds, and logged | F-062; the outdoor probe's validity in the decision's inputs | M6: the decision and its record under both policies, with the probe absent, stale and present | none yet | — |
| H14 | A probe transient on an unclamped input reads as a real state change: the selector flips to manual, a temperature jumps (hardware#31) | An out-of-range or CRC-failed reading is no reading | Selector debounce; F-053 | M5: eleven CRC failures in a row produce no value and one concern | bench 2026-09-16 records the hazard | — |
| H15 | A firmware change locks the probe out of a revision A board for good (hardware#29) | No stop mode, no SWD pin reuse on revision A | F-012 | Review, and the board revision as a build-time type that makes the stop-mode path unreachable on revision A | host: `f_012_*` reads the policy in `o89-core`, 2026-09-18; the type lands with the first low-power path | — |
| H16 | Power restoration at an unattended, unpaired site opens enrolment with nobody at the panel | The temporary revision A exception lasts 120 seconds from boot, ends on first enrolment, and still requires a valid proof; unknown storage never grants it on the repair boot, but the recovered empty table restores eligibility at the next boot | P-066, F-091; revision policy and pre-repair client-table evidence, ordinary panel deadline and L-195 closure | Host revision, storage, expiry, reset and proof fault matrix; lamp/radio/phone bench still required | host: `f_091_p_066_*` in `o89-core` and `o89-sim`; no bench evidence | — |

## Fault matrix

The engineering standard's rows, and where each is answered. A row here is a
promise about coverage; the tests are named in the milestones.

| Failure | Where it is answered |
| --- | --- |
| Sensor missing, stale, implausible or conflicting | H10, H11, H14; the store's validity and provenance (M5); every behaviour's answer for *unknown* (M6) |
| Boot, reset, brownout, watchdog or power loss | H1, H2, H3, H4, H5; the boot order (M1); persistence (M2) |
| Disconnect, malformed frame, duplicate or delayed command | H7, H9; the resynchroniser and CRC (M3); the dedup table and counters (M4) |
| Stuck actuator, or command and feedback disagreeing | `FEEDBACK` against the commanded state; *stop not honoured* and *running, not ours* as states (M8) |
| Timer wrap, full queue, exhausted retries, memory pressure | The 64-bit tick (H9); every table with a named capacity that refuses (M4, M5); allocator-free domain crates (F-081); the two-client BLE cap, bounded HCI health check, and fixed-budget ESP32 radio heap with its unbounded vendor HCI receive queue, temporary 47-byte-per-AP scan result allocation, and outstanding exhaustion/recovery qualification in [the comms architecture](ARCHITECTURE.md#the-comms-processor) (F-037, #89, #96) |
| Interrupted update or corrupt persisted state | H1, H4, H8; the bootloader's trial count and its rollback copy from the NOR, and a NOR lost mid-copy holding the fail state until a probe reaches the unit (M7); the ring's torn-write rule (M2) |
