# Bench session, 2026-09-19

Board A rev A, the same unit and probe as 2026-09-18 (ST-LINK V2-1
`0671FF565357657287183750`, probe-rs 0.32.0), the image at the head of the
M2 stack (`david/m2-recorder`), production layout unless said otherwise.
Instrument: the defmt log over RTT; no analyser on the bus tonight.

## 1. The supervisor from its own interrupt (F-007, F-008, H5)

The review of #40 named the gap: with the FRAM and the NOR driven by
blocking calls behind the async seams, a transfer that never returned
would hold the thread executor, the supervisor on it would never run, and
the watchdog would reset the part with no name to blame. The supervisor
runs from its own interrupt now, `CEC`, at the lowest interrupt priority,
above the thread executor.

The production image boots as before, the supervisor feeding from the
interrupt and the recorder opening the ring:

```text
0.100 [INFO ] store: BootReport { epoch: no epoch: both slots damaged, clients: None, boot: BootCount(49), boot_recorded: Ok(()), panic_recorded: None }
0.206 [INFO ] rail: settled, EN released
0.238 [INFO ] ring: open at block 42 offset 52, next seq 3, oldest Some(1), damage Damage { failed_crc: 0, closed: 0 }
0.241 [INFO ] boot record: seq 3
```

The bench image's starve proof no longer stops checking in cooperatively:
it spins the thread executor with `nop`s, so nothing on that executor runs
again. The proof chain, three boots in one session with the probe's
vector catches off:

```text
0.100 [INFO ] store: BootReport { ..., boot: BootCount(50), boot_recorded: Ok(()), panic_recorded: None }
15.101 [WARN ] watchdog proof: the control tick blocks the executor on purpose; the part resets in about 8 s and the next boot names it
19.200 [ERROR] feed withheld: Control is 99 ms past its window; reset follows
26.200 [WARN ] feed withheld for 7 s; reset pending
0.000 [INFO ] boot: BootRecord { cause: Watchdog, blame: Some(Blame { task: Control, overdue: Millis(99) }), rtc: Lse, backup: Valid, revision: A, module_rail_cycled: true }
0.102 [INFO ] store: BootReport { ..., boot: BootCount(51), boot_recorded: Ok(()), panic_recorded: Some(Landed) }
0.000 [ERROR] the previous run panicked at file 0xa70c05b0 line 345
0.000 [INFO ] boot: BootRecord { cause: Software, blame: None, ... }
0.102 [INFO ] store: BootReport { ..., boot: BootCount(52), boot_recorded: Ok(()), panic_recorded: Some(Landed) }
```

With the thread executor held from 15.1 s, the supervisor named Control
at 19.2 s, 5.1 s after that task's last check-in at 14.1 s and 4.1 s
after the executor was blocked, and counted the seven seconds to the
reset; the boot after the watchdog read the blame. The generator lines
were not on an instrument; the firmware's own sampling of the contact is
all this session says about them. That is
the case the 18th's observation did not cover, when the recorder was
blocked in an `await` and the executor was free. The boot after the
deliberate panic read its site, and the panic record landed on the FRAM
under the boot that found it (F-008 on the part, `panic_recorded:
Some(Landed)`), which the 18th could not show because the store was not
there yet.

The bootloader and the production image were flashed back after the
chain; the board is in the production layout.

## 2. The NOR identified once ready

A reset in the middle of a program or an erase leaves the W25Q128JV
powered and busy, and a busy part ignores the identification. The
recorder waits for the part to report ready, against the erase deadline,
before it asks; the boots above identified it at 0.101 s as before.

## 3. The bench tool through the mailbox (#4, F-014)

The image of `david/m2-dev`, production layout, the same probe. The
recorder serves a mailbox in the last 4 KB of RAM and the host tool
(`crates/o89-dev`, probe-rs 0.32.0 as a library) drives it over SWD while
the firmware runs; nothing here was written around the firmware.

The part as it was, its FRAM still holding the self-test's bytes:

```text
$ just dev-ping
boot 54
$ just dev-store
secret      Corrupt
epoch       Corrupt
boots       Present(BootCount(54))
challenges  Corrupt
volume      Corrupt
run         Corrupt
panics      Present(PanicRecord { boot: 52, words: Panicked(PanicSite { file: 2802582960, line: 345 }) })
release     Corrupt
network     Corrupt
clients     Corrupt
$ just dev-rail a
pin  DrivenHigh
rail On
```

The rail readout is the pin and the rail as two fields (#4): the
firmware had taken `PC5` and driven it high. The revision B case, a pin
nobody drives read as the rail on, is the host test
`f_014_a_port_without_its_clock_or_a_pin_left_analog_is_high_impedance_which_is_on_for_revision_b`;
no revision B board was on the bench.

The epoch and the secret written through the firmware's own seam, each
refused the second time:

```text
$ just dev-write-epoch 1
epoch 1 written
$ just dev-write-epoch 1
Error: the part holds epoch 1; a step back would leave a client table stamped above the record
$ just dev-write-secret
device id      ac89…ca53
printed secret (shown once, not kept here)
$ just dev-write-secret
Error: the part holds a secret; --replace orphans every client enrolled under it
$ just dev-fram 0 32
00000000  45 50 4f 43 01 00 00 00 01 00 00 00 bf df 13 57
00000010  00 00 00 00 00 01 00 00 00 00 00 00 00 01 00 00
```

`EPOC`, sequence 1, body 1, CRC: the epoch record in slot A as
`o89-core` frames it, because the host framed it with the same code. A
reset through the mailbox, and the boot that follows finishes what the
bench started, clearing the corrupt client table under epoch 1 (F-026):

```text
$ just dev-reboot
rebooted: boot 54 to 55
$ just dev-store
secret      Present, device id ac89…ca53
epoch       Present(Epoch(1))
boots       Present(BootCount(55))
clients     Present(ClientTable { epoch: Epoch(1), rows: [None, None, None, None, None, None, None, None], dedup: … })
```

The NOR through the ring: block 0, inside the ring's span but holding
the self-test's bytes before the ring's first record, erased and read
back; a block past the part and a FRAM read past its end refused:

```text
$ just dev-nor 0 16
00000000  a5 01 1f 00 00 00 00 00 00 00 00 01 00 04 ff 00
$ just dev-erase-nor 0
erased block 0
$ just dev-nor 0 16
00000000  ff ff ff ff ff ff ff ff ff ff ff ff ff ff ff ff
$ just dev-erase-nor 4096
Error: erasing block 4096: out of range for the part or the mailbox
$ just dev-fram 32760 16
Error: past the end of the FRAM
```

The boot after all of it, with the log on the probe: the store boots
with its epoch, the ring's head is where it was, the erase of block 0
having moved nothing the ring reads:

```text
0.101 [INFO ] store: BootReport { epoch: epoch 1, clients: Some(Ok(rebased)), boot: BootCount(56), boot_recorded: Ok(()), panic_recorded: None }
0.238 [INFO ] ring: open at block 42 offset 234, next seq 10, oldest Some(1), damage Damage { failed_crc: 0, closed: 0 }
0.241 [INFO ] boot record: seq 10
```

The controller image is 60 112 bytes, 2 104 more than at 889e8c7.

## 4. The link task on USART1, and the ladder for real (F-030, F-006, L-110, L-111, H3)

The image of `david/m3-link-controller`, production layout, the same
probe. The module carries the old scan app, which talks at the link's
baud and speaks nothing `km43` frames, so nothing on the other end can
state itself: the controller's side of the ladder runs for real, and
every byte the module sends is counted as noise.

**The DMA ring first, and it overran from the first byte.** USART1's
receive on `DMA1` channel 2 and its transmit on channel 1, each on an
interrupt line of its own, so as not to repeat the recorder's finding on
the shared `DMA2` line (#39):

```text
0.207 [INFO ] link: the rail settled; USART1 up
0.227 [WARN ] link: receive overrun
0.227 [WARN ] link: receive overrun
…
```

16 102 overruns in 190 s, one every read, the ring never draining the
data register: the same failure as I2C2 and SPI1 on `DMA2`, on `DMA1`
lines of their own, which points below the interrupt lines, at the DMAMUX
routing or the channel setup in `embassy-stm32` 0.6.0 for this part
(#39). The link's own machinery ran through it: the statement retried
and given up every two seconds, the cut at sixty seconds, the cycle
recorded, the ROM text counted.

**The interrupt-driven UART, no overrun in three minutes.** The same
image with `BufferedUart` and rings of two frames in and one out:

```text
0.207 [INFO ] rail: settled, EN released
0.207 [INFO ] link: the rail settled; USART1 up
0.345 [INFO ] record 0x0601: seq 19
1.827 [WARN ] link: RequestFailed(0x60)
3.867 [WARN ] link: RequestFailed(0x60)
…
60.378 [WARN ] link: asking the rail for a cut
60.382 [WARN ] rail: cutting for 5000 ms, cycle 1 in the last hour
60.383 [WARN ] link: PowerCycled { count: 1 }
60.449 [INFO ] record 0x0802: seq 20
65.382 [WARN ] rail: power restored, 1 cycles in the last hour
65.482 [INFO ] rail: settled, EN released
65.482 [INFO ] link: the rail settled; USART1 up
65.483 [INFO ] link: RomText { count: 12662 }
125.496 [WARN ] link: asking the rail for a cut
125.503 [WARN ] link: PowerCycled { count: 2 }
125.548 [INFO ] record 0x0802: seq 21
130.603 [INFO ] link: RomText { count: 12205 }
190.743 [WARN ] link: asking the rail for a cut
190.763 [WARN ] link: PowerCycled { count: 3 }
190.848 [INFO ] record 0x0802: seq 22
```

Zero overruns. The `LinkUp` goes out the moment the rail settles and is
given up after its three attempts, then again every two seconds, ninety
times in three minutes; the cut comes 60.17 s after the rail settled,
the rail is off 5.0 s and settles 100 ms after it returns, and the next
cut comes 60.0 s after that. Each cycle is a class A record on the ring,
sequences 20 to 22, its count on the probe until km43#32 gives the
record a body. The ROM-text count per module boot is twelve thousand,
which is the old app talking and not the ROM; the count near one that
F-031 expects comes with the comms firmware of the next cut. The lines
were not scoped: what the firmware asked of the pins is on the log, what
the pins did is not.

The controller image is 85 728 bytes, 25 616 more than at 633cbee: the
state machine, `km43`'s link-local bodies and CBOR, and the buffered
UART driver.

**The rail before the store.** After the review of #45, the boot powers
the rail before any bus and waits its settling before the FRAM, so a
reset inside a cut adds only the path to the rail (F-005). The module
carries the comms firmware of #47:

```text
0.000 [INFO ] boot: BootRecord { cause: Software, …, revision: A, module_rail_cycled: true }
0.000 [INFO ] rail: powering the module, EN held low
0.201 [INFO ] store: BootReport { epoch: epoch 1, …, boot: BootCount(82), boot_recorded: Ok(()), … }
0.228 [INFO ] rail: settled, EN released
0.228 [INFO ] link: the rail settled; USART1 up
1.810 [WARN ] link: RequestFailed(0x60)
2.006 [INFO ] link: RomText { count: 1140 }
```

The rail goes on in the log's first millisecond, and the store follows
after the 100 ms settle. The log's clock starts with the clocks, so the
path before it is not measured: the bootloader, the reset vector and the
clock start, with no bus and no wait but the oscillator's. The first
`LinkUp` falls in the module's window and is given up; the next is
answered, and nothing failed or was cut in the 43 s after it.

## 5. The flashing path through the controller (F-038, F-034, H8)

Board A revision A, controller image 93 992 bytes at the end of the
session, comms image 110 736; probe-rs 0.32, esptool 5.4.0 over the
bench tool's socket relay, ROM loader only (`--no-stub`), 115200 baud on
the bridge. The module's UART0 is bridged to two rings in the mailbox
that the host reaches over SWD.

**Two lamps lit, and a controller that ran nothing.** Before the first
run, LED2 and LED3 were fully lit and the mailbox answered nothing. The
core was halted at probe-rs's flash loader in RAM (PC `0x2000001e`, the
loader's `bkpt` pair, every other register at its reset value):
`probe-rs download` leaves the part there, and `just flash-controller`
never reset it. Every pin an analog input, so two of the three RS-485
transceivers read their floating DI low and drove their buses low, the
lamps showing it, which is hardware#28 observed rather than inferred; the
third DI floated high. Both recipes now end with a reset.

**The ROM never waits in its loader by itself.** The module's flash had
lost its app to an earlier run that erased and then dropped esptool's
socket, and kept its bootloader and partition table. Listened to through
the bridge, the ESP-IDF bootloader printed its table, `No bootable app
partitions`, and rebooted, forever (`rst:0x3 (LP_SW_HPSYS)`), and the
ROM's loader was never entered. The no-knock entry the tool had, built on
the belief that an empty module waits in its loader, was removed.

**The strap route.** With IO9 driven low by PC3 across an EN reset from
the rail sequencer, held 50 ms with EN and 100 ms after its release, the
ROM printed `boot:0x0 (USB_BOOT)` and `wait usb download` and nothing
after: the strap is read, and with IO8 floating it selects the USB-only
loader, as on 2026-09-14. The bridge caught the banner only once the
sequencer reported the rail settled at EN's release rather than at the
strap's; before that change the run showed 0 bytes. With the wire from
CN11 pin 2 held on U8 pad 10, the same reset entered the serial loader
and esptool wrote 176 240 bytes at `0x0` in 25.1 s (56.2 kbit/s), hash
verified, and the module booted the image: `link: up` with
`fw: o89-comms 0.0.0`, `boot_id 0x5939bb7a`, `RomText 1141`. One
attempt of three failed on the wire's contact (`No serial data
received`), the next succeeded.

**The window route, and the watchdog.** Through the window with no
wire, the module answered the knock and entered its loader
(`the module is entering download mode; bridging at 115200 baud`),
esptool connected, erased `0x0` to `0x2bfff` in 0.43 s, and then the
socket went quiet: `No more data to read from the serial port`, the
controller logging `3 bytes from the module were lost on a full ring`
9.2 s after the module's reset. The module's RTC watchdog, armed at 8 s
as the firmware's first statement (F-032), lives in the LP domain and
outlived the software reset into the ROM, whose loader does not feed it:
it reset the chip out of the loader after the erase and before the
first write, and the app was gone again. The window now disarms it
before the reset (F-034). The module was recovered by the strap route
with the wire, 176 272 bytes in 25.1 s, and then flashed through the
window with no wire: 176 272 bytes in 24.7 s (57.1 kbit/s), hash
verified, the module reset normally, and `link: up` 1.8 s after the rail
settled with `boot_id 0xce6de451` and `RomText 1142`. Then five more in
a row through the window, unattended: 24.6 to 24.7 s each, every hash
verified, the link up after each with a new `boot_id` (`0xf80a11fd`,
`0x478725ac`, `0x4484fd41`, `0x35e171f2`, `0xcef6ddc0`), nothing lost on
the rings. The ROM-text count per module boot is about 1 140 with this
bootloader's log, and F-031's baseline is now that number rather than
one.

**A host that dies with the bridge open.** A host that exits without
giving the module back, as one that crashed or lost its probe would
(`dev-comms-listen --leave-open`), left the module held in its ROM with
the bridge open. Killing the tool instead left the ST-Link's USB
interface claimed with no process holding it, and only a replug freed
it, so the fault is injected by a clean exit. Two answers, each run on
the board:

- A flash through the window 4 s later reclaimed the bridge
  (`another download reclaims the bridge`), wrote 176 272 bytes in
  24.6 s with the hash verified, gave the module back, and the link was
  up 1.8 s after. An earlier image lost the answer to the host's
  request for the normal state, because two reports went out back to
  back and the second replaced the first; reports now go out once per
  request, under the request's number.
- With nothing after it, the bridge closed by itself 60 s after the
  host's last lease renewal (`the host's lease on the bridge lapsed 60 s
  ago`), the module was reset normally, and the link was up 1.8 s
  later. An earlier image measured the idle from any byte on the bridge
  and never closed, because the module's application talks on the link
  whether anyone listens or not; the host now renews a lease word in the
  mailbox about once a second while it reads the bridge, and only that
  counts.

The controller image grows from 87 144 bytes at the comms cut to
93 992 (+6 848): the bridge, the knock, the strap route, the mailbox's
rings, and the bridge's lease and reclaim. The comms image grows by 32
bytes to 110 736 for the disarm.

## 6. The link measured by answers (L-033, L-100, F-038)

After review the link rules changed at the source (origin89hq/km43#52):
a side is linked once its own `LinkUp` is answered, and silence is
measured from the peer's last answer to one of its requests, so a comms
processor that talks but cannot hear is cut at sixty seconds instead of
never. Both firmwares follow it; controller image 94 520 bytes, comms
image 110 704.

Against the previous comms image, which still linked on either
statement, the new controller linked at 1.98 s. With both new images,
flashed through the window with no wire, 176 240 bytes in 24.6 s and
the hash verified, the link came up once, 1.8 s after the module
returned, `RomText 1141`. The run showed two faults of the first
round-trip build, both fixed before this note: the adapter logged the
link up when the peer's own statement was recorded, which no longer
links; and a flash left the state machine linked while the module sat
in its ROM, so the first tick after the bridge counted forty seconds as
silence and recorded comms link lost (`0x0801`). The bench now takes
the module explicitly: every connection drops as `ModuleTaken`, nothing
is recorded as lost, and the controller logs the reason with the
verdict as L-192 asks (`EnterDownload (bench) answered entering`).

## 7. The comms firmware on `o89-comms-core`, and the module's lines through its reset (#47, F-033, L-015, L-100, L-120, L-192)

The images of `b0bda0d`, the head of the stack after the second review
of #44 to #51: the comms firmware boots from the HAL's bare entry and
enters its executor only after the window; its window and link are
`o89-comms-core`'s, on the link mechanics `o89-link` shares with the
controller; the knock takes only an answer under its own id and a
refusal is reported as one (L-192); the rail comes on before the store
(F-005). Controller image 97 048 bytes, comms image 112 832.

**A reset loop, and the window through it.** A build of this cut that
confirmed its OTA slot three times over and reset when it could not read
the slot's state was flashed first. On this module, written as the
factory image with no slot selected, that read is an error by design, so
the module reset after every window: the controller logged its `LinkUp`
failing every two seconds, the cut at sixty seconds, and 42 034 bytes of
boot text in the minute before it, a boot every 1.6 s. The window ran on
every one of those boots and answered the knock through the loop: the
corrected image, which reads no selected slot as nothing to confirm, was
written and hash-verified with no wire, and linked. The recovery path
held against a firmware of ours that could not boot.

**The flash, answered under the knock's own id.** The controller logged
`download (Bench) by Knock`, then `EnterDownload (Bench) answered
entering` 270 ms after it released `EN`, the answer from the window of
the image before, the first built from the bare entry; the recipe built
the images first, and esptool wrote 178 368 bytes in 24.9 s, the hash
verified. Given back, the module linked 1.78 s after its reset with
`fw: "0.0.0+gb0bda0d6"`, and nothing failed, dropped or was cut in the
minute read after it. The ROM text on that boot counted 915 against
1 142 on the controller's own boot a minute later; the count is of runs
the reader drops in text at another baud, and moves with that text.

**The comms side stays linked on answers to its own beats.** A build of
the controller that is not committed logged each frame the module sent,
by opcode and request id. After the link came up at 2.0 s the module
sent its `Heartbeat` (`0x61`) and answered the controller's (`0xE1`)
every 2 s, 14 of each in the 26 s read, and not one `LinkUp`: its
six-second timer, which only the answer to one of its own beats resets
(L-100, L-120), never fired.

**The module's lines through its reset.** The same build sampled `PB4`,
the controller's CTS on the module's GPIO5, and `PB7`, its RX on the
module's GPIO16, every 500 µs for four seconds from the rail's
switch-on, each read under the controller's internal pull-up and then
its pull-down: a line that follows both is floating, one that reads the
same under both is driven.

```text
0.000 rail: powering the module, EN held low
0.000 probe: PB4 floating; PB7 floating
0.223 rail: settled, EN released
0.231 … 0.393: PB7 driven high, low and floating in turn; PB4 floating
0.394 probe: PB4 driven low; PB7 driven high
```

Held in reset, the module drives neither line, as the ESP32-C6
datasheet's table 2-1 has it. Released, GPIO16 carries the ROM's
banner, sampled mid-character, and GPIO5 floats for 171 ms, until the
firmware's second statement takes it as RTS and asserts it; both are
driven from then on. The images of `a43f580` and `856f06b`, before the
bare entry, read 186 and 185 ms. The controller's first `LinkUp` went
out into that floating CTS and was not held past its deadline: no stall
was logged. A brown-out that resets the module is the first state, and
with the rail off the controller's USART1 is down (F-003).

## 8. The ladder's count across a controller reset (#49, F-017, F-018, L-112)

The controller image of `2e513ff`, 101 320 bytes, the ladder's cuts of
the last hour kept on the FRAM. To make the ladder climb, the module ran a
comms image built for this run only, never committed, whose link sends
nothing: its window still ran, so it was flashed in through the window and
out again the same way.

**One cut at boot, one at sixty seconds.** The first boot of the image
found no record, counted its own reset as a cut because revision A's rail
is off through every reset (F-018), and kept it:

```text
0.210 [INFO ] rail: 1 cuts in the last hour, carried and this boot's
0.367 [INFO ] rail: 1 cuts in the last hour kept
60.298 [WARN ] link: asking the rail for a cut
60.311 [WARN ] rail: cutting for 5000 ms, cycle 2 in the last hour
60.313 [INFO ] rail: 2 cuts in the last hour kept
60.368 [INFO ] record 0x0802: seq 85
```

The cuts were on the part 2 ms after the sequencer decided, in the same
turn of the rail task and before its lines were applied.

**A reset between rungs, and the third rung.** The controller was reset
through the probe. The boot after it carried the two cuts as made at its
own start and counted its own reset, and the next request, at sixty
seconds of silence, was the third rung:

```text
0.204 [INFO ] rail: 3 cuts in the last hour, carried and this boot's
0.361 [INFO ] rail: 3 cuts in the last hour kept
60.292 [WARN ] link: asking the rail for a cut
60.305 [ERROR] rail: the third rung; left on, comms unrecoverable
60.363 [INFO ] record 0x0803: seq 87
```

Before this change the same reset started the count again at one, and
the ladder cut the rail every minute without end. The module was then
flashed back to the committed comms image through the silent image's
window and linked. The lines were not scoped: the log shows the order the
firmware asked for, not the order the pins moved in.

## 9. The ladder's cut path, the counts that do not land, and a download's reset racing one (#54, F-017, F-018, L-111, L-112)

The controller image of `a891911`, 103 000 bytes, with the same comms
image that never answers as §8, flashed in and out through the window.
The probe's log is the only instrument: the lines were not scoped.

**The record of the older format, and the full ladder.** `LADR` grew by
four bytes with #54, so the record written before it no longer reads, and
the boot counted the three cuts a record that does not read stands for.
Every request for the next hour was answered with the third rung, in the
turn it arrived:

```text
0.206 [INFO ] rail: 3 cuts in the last hour, carried and this boot's
133.740 [WARN ] link: asking the rail for a cut
133.747 [ERROR] rail: the third rung; left on, comms unrecoverable
133.816 [INFO ] record 0x0803: seq 89
```

**The cut made on the turn its count landed.** An hour in, the carried
cuts aged out and the part was written twice as they did. The next
request planned a cut, its count landed 31 ms later, and the rail went
off in that same turn; the instant the lines moved went to the part on
the turn after, and the rail came back 5 000 ms after it, not after the
request (L-111):

```text
3600.027 [INFO ] rail: 1 cuts in the last hour kept
3600.227 [INFO ] rail: 0 cuts in the last hour kept
3618.376 [WARN ] link: asking the rail for a cut
3618.407 [INFO ] rail: 1 cuts in the last hour kept
3618.407 [WARN ] rail: cutting for 5000 ms, cycle 1 in the last hour
3618.427 [INFO ] rail: 1 cuts in the last hour kept
3623.407 [WARN ] rail: power restored, 1 cycles in the last hour
3623.507 [INFO ] rail: settled, EN released
```

**The record across a controller reset.** The boot after it read the
record written at the boot before, counted no boot missing between them,
and added its own reset (F-018). A record whose boot count did not agree
would have counted three:

```text
0.208 [INFO ] store: BootReport { epoch: epoch 1, ..., boot: BootCount(96), boot_recorded: Ok(()), ... }
0.210 [INFO ] rail: 2 cuts in the last hour, carried and this boot's
```

**The counts that do not land.** A controller image built for this run
only, never committed, 103 616 bytes: the recorder stalls 2.5 s on the
first new cut's count and refuses the second, and the rail task holds the
third's landed count until a request arrives. Each deferral left the rail
on and the ladder asking again a minute later, and the count this boot
held went back to the part a retry after the write whose landing was not
known:

```text
60.311 [WARN ] BENCH: the recorder stalls 2.5 s on the first new cut's count
62.331 [ERROR] rail: the ladder's cuts not kept: Late
62.331 [WARN ] rail: the cut deferred, its count not kept or the module reset; the ladder asks again
63.351 [INFO ] rail: 2 cuts in the last hour kept
122.411 [WARN ] BENCH: the recorder refuses the second new cut's count
122.431 [ERROR] rail: the ladder's cuts not kept: Refused
122.431 [WARN ] rail: the cut deferred, its count not kept or the module reset; the ladder asks again
123.451 [INFO ] rail: 2 cuts in the last hour kept
```

**A download's reset in the turn the count lands.** With the third cut's
count held, the bench asked for the module through the mailbox, as a
flash does. Both reached the rail task in one turn: the reset was served,
the cut was not made, `EN` went low a millisecond later, and the bridge
opened. The part, which had taken the count of a cut that never happened,
was written back to the count this boot holds:

```text
187.921 [WARN ] mailbox: the host asks for the module's download mode, by Reset
188.011 [WARN ] BENCH: the landed count handed over with Some(ResetModule(Normal))
188.011 [INFO ] rail: 3 cuts in the last hour kept
188.011 [WARN ] rail: the cut deferred, its count not kept or the module reset; the ladder asks again
188.012 [INFO ] rail: resetting the module, EN held
188.071 [INFO ] rail: settled, EN released
188.071 [INFO ] link: the module is entering download mode; bridging at 115200 baud
188.091 [INFO ] rail: 2 cuts in the last hour kept
191.125 [INFO ] link: the bridge is closed; the module reset normally
```

Before #54 that reset came after the cut, found the rail off, and the
download failed. The ladder then went on from where it was: the next
request cut the rail for the third time in the hour, and the one after it
was the third rung.

```text
251.291 [WARN ] rail: cutting for 5000 ms, cycle 3 in the last hour
256.291 [WARN ] rail: power restored, 3 cycles in the last hour
316.451 [ERROR] rail: the third rung; left on, comms unrecoverable
```

The board was then flashed back to the images of `3ffc261`, main's head,
and linked; they were built on `3ddfd7e`, the branch commit that
squash-merged into it, whose tree is the same and whose hash the link
version text therefore carries. What this run does not cover: the lines were not scoped, so the
5 000 ms off and the order the pins moved in are the firmware's account
of itself; a supply cut inside the record's write is on the host only
(the write is cut at each of its 45 bytes there); and a reset before the boot count
itself lands, the first durable write a boot makes, is counted by no
later boot, because the count never advanced and nothing names the boot
that was. A reset after that count and before the ladder's own record is
the case the next boot counts, from the gap in the count.

## 10. The slot flash, and a transfer killed mid-write (#58, F-084, F-036, F-038)

Board A rev A, the same unit and probe. Controller image 103 024 bytes,
comms application image 112 832 bytes; the runs below are at `c354bc7`, and
the last paragraph re-runs the successful path at `7b24d26`, where review
moved the partition lookup from the row's name to the type and subtype the
bootloader matches on. esptool 5.4.0 over the
bench tool's socket relay, ROM loader only (`--no-stub`), 115 200 baud on
the bridge. Instrument: the defmt log over RTT and the module's own UART0
through the bridge; no analyser on the bus.

**The window answered before anything was written.** The factory image the
module carried from §5 was knocked at first, so that the runs below rest on
a recovery image known to work rather than on one assumed to:

```text
  rst:0x3 (LP_SW_HPSYS),boot:0x4 (DOWNLOAD(USB/UART0/SDIO_FEI_FEO))
  waiting for download
```

**The application into `ota_0`, and the bootloader's own account of it.**
`just dev-flash-comms` wrote the blanked `otadata` (8 192 bytes at `0x9000`
in 1.1 s), the application (112 832 bytes at `0x210000` in 16.0 s,
56.5 kbit/s), and then the `otadata` naming the slot (8 192 bytes in 1.2 s),
each hash-verified. Three writes in two esptool runs over one bridge, not
three: the blanked `otadata` and the application go in the first run, and
the `otadata` naming the slot in the second. That boundary is the one the
ordering rests on — everything the second run does is known not to have
happened while the first is still going — and it is the only process
boundary this exercised. The second run re-synchronised with the ROM over
the bridge the first left open, which is what the two-pass order needs and
what had not been tried before.

The module's next boot named the partition it loaded:

```text
  I (60) boot:  1 factory          factory app      00 00 00010000 00200000
  I (66) boot:  2 ota_0            OTA app          00 10 00210000 00200000
  I (96) esp_image: segment 0: paddr=00210020 vaddr=42000020 size=0074ch (  1868) map
  I (146) boot: Loaded app from partition at offset 0x210000
```

So the `otadata` this tool writes is one the bootloader accepts: the CRC
parameters and the `(seq - 1) % slots` rule are right on the part, not only
against the crate they were read from. The factory image is still at
`0x10000` and was never written.

The controller saw the image it had just sent:

```text
0.232 [INFO ] rail: settled, EN released
0.232 [INFO ] link: the rail settled; USART1 up
1.738 [WARN ] link: RequestFailed(LinkUp)
2.006 [INFO ] link: up; the module is Peer { version: Version { major: 1, minor: 0 }, fw: "0.0.0+gea941d73", hw: "controller-a rev A", boot_id: 479441335, net_version: Some(0) }
2.007 [INFO ] link: RomText { count: 1033 }
```

`fw` names the commit that was flashed, which is what makes this the image
under test and not the one that was already there.

**The transfer killed mid-write, which is what F-084 is for.** esptool was
sent `SIGTERM` seven seconds after the blanked `otadata` landed, 37.2 % of
the way through the application:

```text
esptool: Wrote 8192 bytes at 0x00009000 in 1.1 seconds (57.5 kbit/s).
--- blank otadata landed at 19:04:31 ---
esptool: Writing at 0x0021a400 ━━━━━━━━━━━    37.2% 41.00kB/110.19kB [5s]
--- killing esptool at 19:04:38 ---
Error: pass 1 of 2
Caused by:
    esptool failed: exit status: 143
```

The tool reported the pass that failed and returned the module to normal.
With `ota_0` a third written and the `otadata` blank, the module booted:

```text
  rst:0x1 (POWERON),boot:0x8 (SPI_FAST_FLASH_BOOT)
  I (99) esp_image: segment 0: paddr=00010020 vaddr=42000020 size=0074ch (  1868) map
  I (150) boot: Loaded app from partition at offset 0x10000
```

The factory image, which is the claim. Before this change the same fault
left a module that boots nothing, and §5 spent a wire on exactly that.

**Recovered with no wire, in one run.** `just dev-flash-comms` again, against
a module running the factory image: it answered the knock, took all three
writes with every hash verified, and the link came up on the rewritten slot:

```text
2.006 [INFO ] link: up; the module is Peer { ..., fw: "0.0.0+gc354bc70", ..., boot_id: 2125610330, net_version: Some(0) }
2.007 [INFO ] link: RomText { count: 1031 }
```

No wire was placed on the module at any point in this section, and the strap
route was not used.

**Re-run after the lookup changed.** Review found that the tool chose the
slot by the row's name while the bootloader chooses by type and subtype, so
a table naming an unrelated row `ota_0` would have taken the application and
left the bootloader starting elsewhere. The lookup now asks the bootloader's
question, and the successful path was run again on the board at `7b24d26` to
show the roles resolve against the real table:

```text
writing the application into ota_0 (app/ota_0) at 0x210000; the bootloader, the partition table and the factory image stay
esptool: Wrote 8192 bytes at 0x00009000 in 1.1 seconds (57.4 kbit/s).
esptool: Wrote 112832 bytes at 0x00210000 in 16.0 seconds (56.4 kbit/s).
esptool: Wrote 8192 bytes at 0x00009000 in 1.1 seconds (57.5 kbit/s).
2.007 [INFO ] link: up; the module is Peer { ..., fw: "0.0.0+g7b24d261", ..., boot_id: 2455134596, net_version: Some(0) }
```

Every hash verified, and the link up on the rewritten slot. The killed
transfer was not run a second time: what it exercises is the write order,
which the change to the lookup did not touch.

**The module's own table, read back (F-085).** Review found the deeper
version of the same fault: the tool planned from the table in the
repository, while the bytes land on the table the module holds. A change to
`partitions.csv` alone — moving `ota_0` into the range the module still
keeps its factory image in — would have erased the recovery image while the
guard, checking the repository's factory range, reported that it stayed.

The slot route now reads the table out of the sector the bootloader reads
it from and plans from that. The ROM's loader serves the read, which had
not been tried before and is what the fix rests on:

```text
esptool: Read 4096 bytes from 0x00008000 in 2.2 seconds (15.1 kbit/s)
writing the application into ota_0 (app/ota_0) at 0x210000; the bootloader, the partition table and the factory image stay
esptool: Wrote 8192 bytes at 0x00009000 in 1.1 seconds (57.5 kbit/s)
esptool: Wrote 112832 bytes at 0x00210000 in 16.1 seconds (56.2 kbit/s)
esptool: Wrote 8192 bytes at 0x00009000 in 1.2 seconds (56.4 kbit/s)
2.007 [INFO ] link: up; the module is Peer { ..., fw: "0.0.0+g3233adca", ..., boot_id: 607008670, net_version: Some(0) }
```

2.2 s added to a flash, every hash verified, and no disagreement reported
between the module's table and the repository's, which is right: they are
the same table. The run is at `f2b3aef`; the module states `3233adca`
because none of the comms image's own sources changed between the two, so
the same bytes were built, and the version text names the commit those
sources came from rather than the tool's.

Review then asked for the table's own checksum to be verified, since a
corrupted table the bootloader would reject is one this must not plan from.
It is recomputed the way `esp_partition_table_verify` does it, over the
entries ahead of the checksum entry, and the reader is tested against the
bytes `espflash` really lays down, committed as a fixture: the checksum
entry sits 192 bytes in and states `099c14be8ee7262cfdb33a9325c8ec10`, which
is the MD5 of the 192 bytes before it. The risk in adding the check is
refusing a module that works, so it was run against the board: the table
read off the part passed, and the flash went through as before.

The module reported `fw: "0.0.0-dirty+gba2ea456"` on that run. The `dirty`
is correct and worth recording, because it is L-034 working: adding the
checksum crate changed `Cargo.toml` and `Cargo.lock`, which are sources of
the comms image, where the earlier tool-only commits were not and so did
not mark it.

What this does not cover: a module whose table really has moved on, which
would need a whole flash with one table and a slot flash with another. The
host tests hold that case — `f_085_the_plan_comes_from_the_table_the_module_holds`
shows the same plan called safe by the table that moved on and unsafe by
the module's — and it was not staged on the board, because staging it means
deliberately installing a table the repository does not have.

**What this section does not cover.** The whole-flash route's own fault: it
is meant to leave a module that boots nothing, and proving that would need
the IO8 wire in place first, so it was not run. A kill inside the third
write, which would leave the slot whole and the `otadata` blank — the module
boots the factory image by the same argument as above, but it was not run.
The lines were not scoped: what the firmware asked of the pins is on the log,
what the pins did is not. A bootloader with rollback enabled, which is what
#1's acceptance test needs and what cut 6's other half is about; the
`ota_state` written here is `New`, and with the bootloader now on the part it
is read once and set valid.

## Not done

The DMA path (#39) and the thousand supply cuts. The analyser was not on
the bus. A revision B board for the other half of #4. The third rung by
the ladder's own cuts alone: §4's run ended at three, one short of it,
and §8 reached it with a controller reset counted among them; four cuts
inside an hour with no reset between them are run on the host only. The boots whose own record
never landed, counted by the next: on the host only, since a reset inside
that write cannot be aimed at the board. The acceptance test of #1, an image that
crashes at once delivered as an update and recovered through the window,
which needs the rollback-enabled bootloader of the next cut. The bench
tool wrote the factory slot on every run until §10, where it stopped and
a killed transfer was shown recovering with no wire. The window's edge on the bench, a knock
that begins after 1.5 s: the host tests hold the boundary, and the tool
has no way to knock late.
