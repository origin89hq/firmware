# Bench session, 2026-09-25

Board A rev A, ST-LINK V2-1 `0671FF565357657287183750`, equipment
disconnected, board B not attached, no wire on the module. The session ran
from 2026-09-24 22:00 UTC into the morning. The controller ran 6f459be and,
from about 00:30 UTC, 7597a4d's build, which is the same controller code.
Comms images were written into an OTA slot by `just dev-flash-comms` or
`o89-dev flash-comms <elf>` through the controller, by the knock. The BLE
client was the Mac, using `bleak` 20 cm from the board: a 20 s connect,
then service discovery, notify on TX, and 10 s connected. The client table
was empty (epoch 4), so every controller boot opened the first-enrolment
window for 120 s (F-091).

## Three traps that invalidated runs

Each looks like a firmware fault. The results below come only from runs
that avoided all three.

- **The controller sits halted after a probe reset (#155).** After
  `just reset-controller`, the core does not run until the next
  `just attach-controller`. Until then there is no link and no advertising,
  and the mailbox times out. Runs must keep an attach open from the reset
  onward. The cause and the fix are in the last section below.
- **Resetting right after a comms flash rolls the module back.** The new
  image confirms its slot only after its download window. A controller
  reset about 1 s after `module reset normally` cycled the module's rail
  first, and the module booted the other slot's Sep 22 image,
  `0.0.0+gce81fbe7`. That image has an old storage layout: it reported
  `net_version` 0, answered `NvsWriteFailed` and left `PairingWindow`
  unanswered. Wait about 25 s after a flash and check `fw` on every run.
- **An outside BLE client held the slots.** From about 01:55 UTC even the
  Wi-Fi-off control advertised nothing. A phone connecting without
  subscribing held both slots, which is fixed below.

## The access point reset the module every 6.8 s (#160)

While the window was open, the radio ran the access point beside the
station. The module rebooted every 6.8 s from link-up until the window
closed at 120 s, and the link came back each time under a new `boot_id`.
The reset reason, read back through a patched version string, was
`CoreSw` (0x03): a software reset or a panic. That rules out brown-out
and the watchdog. It reproduced on 7ed5cf6 (before #156) and on 8aebdab,
and it did not happen on 7597a4d, which drops the access point.

## BLE and a running station do not share the radio (#161)

| Image | Advertising during the window | Connect and subscribe |
| --- | --- | --- |
| Wi-Fi never started | continuous, 5 to 19 per 5 s for 90 s | 3 of 3 |
| Station joined (V1, V2, V3) | continuous | 0 of 12, each a 20 s timeout after the advertisement was found |
| Station disconnected at the window (V4) | stops at the disconnect, 6.1 s | none to try |
| Station session ended without disconnecting (V4b) | continuous | 0 of 3 |
| Station stopped by `esp_wifi_stop` (V5) | stops at the stop, 6.1 s | none to try |
| Wi-Fi off during the window (6c28963) | continuous | 3 of 3 |

V2 added forwarding of esp-radio's stubbed BLE coexistence callbacks, and
V3 added Wi-Fi modem sleep on top of it. Neither changed the result, so
neither shipped. Upstream: esp-rs/esp-hal#6397. Advertising lost at a
disconnect or a stop did not return until the module rebooted. Starting a
station and joining did not affect it.

On d1c3ade's image (`0.0.0+g56488aca`, 56488ac, merged with #161):

- Advertising was continuous through the window, and a client connected,
  discovered both characteristics, enabled notify on TX and stayed
  connected.
- After the window closed at 120 s, `dns-sd -G v4 origin89.local` answered
  192.168.0.180 from 134 s to 245 s, checked every 10 s. The controller
  recorded a time set by `NtpViaComms` at 125 s and at 156 s in two runs.
- A client that connected without subscribing was dropped after 10.3 s,
  and one that subscribed stayed.

Not done: the reboot when a window opens while the station runs, since
there is no selector on this bench to open one, and an iPhone pairing,
which the app's stored enrolment blocks (origin89hq/apps#23, #24).

## The controller flash left another watchdog record (#125)

`just flash-controller` from 7597a4d's tree failed to clear the IWDG
freeze ("power-cycle the controller"). The boot after it, boot 243,
recorded `cause: Watchdog`. `DBGMCU_APB_FZ1` read `0x1000` afterwards and
was cleared with `probe-rs write` to 0.

State left: the controller on 7597a4d's build and the module on
`0.0.0+g56488aca`, linked, with the station joined outside the window.
The device secret was replaced on 2026-09-24 with
`o89-dev store write-secret --replace` (device id `93d54da0…`), and the
client table is empty under epoch 4.

## A stale vector catch held the core at its reset (#155)

Same board and probe, 12:20 to 12:42 UTC, the controller on 7597a4d's
build, boots 287 to 294. Registers read with `probe-rs read`, and the core's
registers with a throwaway probe-rs program that halts, reads and resumes.

Before any reset, DHCSR read `0x01040001` and DEMCR `0x00000401`:
`VC_CORERESET` and `VC_HARDERR` armed. `probe-rs attach` and `probe-rs run`
arm both, and neither disarms them when it ends. Their STM32G0 teardown
only turns the DBGMCU clock off, and a system reset keeps DEMCR. Every later
reset, the firmware's `Reboot` included, stops the core on its first
instruction. `probe-rs attach` resumes a halted core as it connects, which
is why the part always came back at the next attach.

- Reproduced three times out of three: DEMCR written to `0x401`, then
  `o89-dev reboot` from main timed out on `Ping`, with the core halted at
  PC `0x080000c0`, the reset handler at `0x00000004`.
- Each time, the fixed `o89-dev ping` said the core was held at its reset,
  ran it, and answered the next boot count (290, 291, 292). DEMCR read 0
  afterwards and RCC_APBENR1 `0x18400401`.
- With DEMCR at `0x401`, the fixed `o89-dev reboot` went from boot 292 to
  293, and `just reset-controller` through the wrapper left DEMCR at 0 and
  the part answered boot 294.
- `just attach-controller` with `--no-catch-reset` was not run.

A second failure turned up along the way. Twice the core hung in
`embassy_stm32::init`, at PC `0x080114a6`, polling PWR_CR1 for DBP. PWR_CR1
read `0x208`, RCC_APBENR1 read `0x08000000` (DBGEN set, PWREN clear) and
SYSCLK was still HSI16. It sat there for minutes, because the IWDG is armed
only after the clocks. The second time, the first version of the fix caused
it: that version ran the core and ended its session straight away. probe-rs's
STM32G0 sequences set DBGEN in RCC_APBENR1 when a session starts and clear it
when the session ends, and each is a read-modify-write over two SWD
transactions. One that lands just after a reset can write back the value it
read before the firmware set PWREN. The fix now keeps its session open
until the firmware serves its mailbox. `just reset-controller` recovered
the part both times. The hang before the watchdog is #168.

## Wi-Fi off with no network until a client scans (#166)

Board A, the same probe. Afternoon, 14:30 to 14:40 UTC. The controller was
on 2ecada9's build, boots 301 to 303. The module ran 3f083a9
(`0.0.0+g3f083a93`), flashed by `just dev-flash-comms`, and each run checked
`fw` in `link: up`. The client was the Mac with the origin89hq/apps bench
at b77d40f (`just bench`, CoreBluetooth, filtering on the KM43 service),
about a metre away. The network section was at version 4, holding no network
and country CA. `store write-epoch 9` cleared the client table, so boot 303
opened the first-enrolment window.

- **Window closed, no network, before any pairing (boot 301):** the Mac found the unit in
  1.9 s, connected and subscribed. On d1c3ade's image it found nothing in
  this state.
- **Pair in the window (boot 303):** connected in 0.6 s and paired, which closed
  the window. The Mac then reconnected with the window closed, in 2 s.
- **`WifiScan` accepted over that connection:** the Mac's link ended in a
  supervision timeout 1.43 s later, and the controller logged
  `CommsRebooted` at 17.191 s. That matches #170's 17.184 s. The reset
  is not one this change plans: with the result outstanding the station
  stays up.
- **After that reboot:** the Mac found the unit again in 3.7 s and read the section
  over BLE.
- **The network written over BLE:** `Stored` version 5 at 88.242 s. The
  Mac's link ended in a supervision timeout 4.4 s later, `CommsRebooted`
  at 93.894 s, and after the reboot the station joined. `dns-sd -G v4
  origin89.local` answered 192.168.0.180.

So the unit can be reached over BLE with no network, and onboarding over BLE
reaches a joined station. Both runs where the Wi-Fi driver started while a
client was connected over BLE also rebooted the module, once for a scan and
once for a join, and dropped the client. The reset site was not captured.
That is #170. This run cannot tell whether the link was lost to the reset
or to the radio.

Not done: the reboot back to Wi-Fi off after the last client leaves a
scan-only session, which the #170 reset preempted, and an iPhone.

State left: the module on `0.0.0+g3f083a93`, the station joined on network
version 5, the client table holding the Mac bench under epoch 9. The
iPhone's enrolment was dropped by the epoch rebase.

## The radio heap ran out beside a BLE connection (#170)

Board A, the same probe. 14:45 to 15:07 UTC. The controller was on 2ecada9's build,
boot 303. The client was the Mac bench as above, with two
uncommitted commands (`clear`, and `scan --no-refresh`). The module ran b03bfb3 with
an uncommitted diagnostic. Every reset site, and the panic handler, left
a code in persistent RTC memory. The next `LinkUp` reported it in the fw
text: the reset reason, the radio's last step, the BLE worker's step,
the site and line, and a hash of the panicking file's path. A later image
also reported esp-alloc's peak use (`internal-heap-stats`).

With the 72 KiB heap, both resets were the same panic,
`alloc/src/alloc.rs:573`: the default allocation error handler. It
panicked inside `scan_async` for a scan and inside `connect_async` for a
join, each with the BLE client in its session.

With the heap at 160 KiB, and then at 128 KiB for three rounds of a scan
over BLE, a write over BLE and a clear over Wi-Fi, the module did not
panic. Every reset was the planned end of a session. The peaks:

| Operation | Peak |
| --- | --- |
| Scan beside a BLE client, no network | 73 KiB |
| Join beside a BLE client, then a WebSocket client | 77 to 78 KiB |

The BLE client still lost its link each time: a supervision timeout within
a second of the station starting to scan, and about two seconds into a
join. The scan completed, the controller kept its list, and the module
rebooted into Wi-Fi off once the result was acknowledged. The Mac then
reconnected and read the kept list with no refresh. Every join completed,
and the clear that followed went over Wi-Fi.

### What the heap explains of the coexistence runs (esp-rs/esp-hal#6397)

11:10 to 11:35 local time, the same setup. The diagnostic image also advertised
with the station joined, and let a session end without the reboot. Each
connection held 10 s, then sent a request.

| Heap | State | Result |
| --- | --- | --- |
| 72 KiB | station joined at boot | 0 of 3 connected: found, never ready |
| 128 KiB | station joined at boot | 3 of 3 |
| 128 KiB | station started and joined after BLE, driver restarted in place | 3 of 3 |
| 128 KiB | station joining a network that is not there | 3 of 3 short reads; one write attempt dropped at 2.5 s |
| 128 KiB | refresh scan over BLE, station already joined | 3 of 3, link kept |
| 128 KiB | driver dropped with Wi-Fi then off | 0 of 3: no peripheral found |
| 128 KiB, `main` otherwise | station up for scans alone, no network, window closed | 0 of 3: no peripheral found |

So the twelve failed connections of 2026-09-24 were the heap. A station
brought up for scans alone, a station started beside an open connection,
and a stopped driver still take BLE down at 128 KiB.

Not done: an iPhone, a heap between 72 and 128 KiB, and exhaustion
recovery under F-037.

On 56f2ce4 (`0.0.0+g56f2ce49`), with no network and the window closed:
- **Read over BLE:** worked.
- **Refresh scan:** dropped the link, then one planned reboot.
- **Reconnect:** the Mac read the kept list.
- **Write over BLE:** the station joined at 192.168.0.180 and the Mac saw the join.

No other reset.

State left: the module on 56f2ce4, the station joined on network version 22,
and the Mac bench enrolled under epoch 9.
## Key agreement on board A (#176)

Afternoon, same board, probe and bench. The controller ran #176 at 9d6115a
(`0.0.0+g9d6115a2`, 242,336 bytes). The module was not reflashed and ran
`0.0.0+g56f2ce49`. The client was the Mac over BLE: a scratch binary using
km43's own client half at 08040fa through `btleplug`, writing without
response to RX and reassembling notifications with km43's `BleReceiver`.
Controller logs came from `just attach-controller`.

**Onto the new map.** The first boot on 9d6115a found no device secret and
kept the link down, and its boot "repaired" the client table over bytes the
old map had written (epoch 9 to 10). `o89-dev store blank --yes` zeroed
17,974 bytes and rebooted onto boot 1 with every record absent;
`o89-dev store write-secret` then gave the unit a key, a generator and a
137-character v2 label, and rebooted onto boot 2.

**Pairing and sessions.** With the table empty, the boot opened the window.
Over one connection: `Discover` answered in 0.06 s (provisioned false,
window open); `Pair` message 2 came back 1.46 s after message 1; `Enrol`'s
answer 1.08 s after message 3, with client 1, generation 1. A new `Discover`
then showed the unit provisioned and the window closed. Five `Hello`s over
two connections, the last three from the enrolment the client had saved,
each answered in 2.45 to 2.49 s, bound as client 1, reported
`capabilities 0x100` (bit 9 clear, P-246), and ended with a sealed `Goodbye`
whose answer opened under the session. One boot record in the log across
all of it: nothing reset.

**The control tick through a `Hello` (P-243).** An uncommitted patch logged
each 1 Hz tick with the widest gap between its 10 ms selector samples.
Across three back-to-back `Hello`s, about 7.4 s of compute, every tick
landed on the same millisecond of the second and the widest sample gap was
17 ms. The one 46 ms gap was during boot, before any client connected. The
patch was reverted and 9d6115a reflashed.

The QEMU instruction counts in origin89hq/km43#130 put a `Hello` at 1 to 1.4
s; the board takes 2.46 s, and pairing's two messages run about 2.2 times
their estimates as well. The rollcall's 10 s window for the worker covers
it four times over.

**The supervisor did not run while control spun (fixed in #176).** The
watchdog proof (`just run-controller-bench`) starved the control executor
on purpose. The watchdog reset the part, but the next boot read no last
words and the supervisor had logged nothing. With the core spinning, ICSR
read `0x0041f018`: exception 24 (`USB_UCPD1_2`, the control executor)
active and `TIM2`, the time driver, pending behind it; every NVIC priority
read 0. embassy-stm32 builds `Priority` for four bits on every STM32, so
`P2` and `P3` are `0x20` and `0x30`, and the M0+ keeps only bits 7:6. With
the executors at `P8` and `P12`, the supervisor named Control 15 ms past
its window at 20.29 s, withheld the feed, and the watchdog reset the part
about 8 s later; the next boot read `Starved(Control, 15 ms)`, the panic
proof after it read its site, and the boot after that ran. Until this
change, main had the same zero priorities and never needed them: control
spun in thread mode, which any interrupt preempts.

**Traps.**

- `o89-dev` reaches the mailbox through the same ST-LINK, and an attach
  holds it exclusively. Stopping the attach from outside left DEMCR at
  `0x401` and the core halted (DHCSR `0x00030003`). Writing DEMCR to 0 and
  `probe-rs reset` recovered it every time.
- `o89-dev store blank`'s first run zeroed and rebooted the part, then
  reported failure: the reboot check required the boot count to rise, and a
  blank starts it over. Fixed in #176 with a reboot that expects boot 1.
- `just dev-store` puts its arguments before `store`, so a subcommand has
  to be run as `cargo run -p o89-dev -- store blank --yes`.
- **The watchdog proof left the bench image under the bootloader.**
  `just run-controller-bench` links at 0x08000000 and overwrote `o89-boot`
  in bank 1; the `just flash-controller` that followed wrote from 0x08002000
  up only. From then on every reset ran the bench image's vector table into
  the production image's code: a reset loop through a re-entrant defmt log
  with the fault-catching image, a lockup with 9d6115a, and a lockup with
  main's image, each at a PC inside no function's first instruction. Two
  hours went to chasing it as a firmware fault, and an A/B against main and
  9d6115a that could not have told anything apart. The tell was the bottom of
  bank 1: `20022000 08000101` where `o89-boot` is `20024000 080000c1`, with
  bank 2's copy intact. `just flash-boot` then `just flash-controller`
  restored it. main's image, run for that A/B, faulted before its store
  wrote anything: the store read back intact afterwards.

Not done: a phone (origin89hq/apps#34 has no v2 client yet), the Wi-Fi
transport, two clients handshaking at once, any refusal path, and a power
cut during a draw on the part. Those are covered on the host only.

After `o89-boot` was restored, a power cycle booted 52768fc cleanly, and two
more `Hello`s bound in 2.46 and 2.52 s.

State left: the controller on 52768fc (`0.0.0+g52768fce`) behind `o89-boot`,
and the module on `0.0.0+g56f2ce49`, linked. The unit holds device id
`f57c05ff…`, controller fingerprint `8cf2dbdf…`, and one enrolment in slot 1
under epoch 1, whose key is the scratch client's.
